<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/main.css">
    <title>Document</title>
</head>
<%
    function progressCalc(list){
    var completedCount = 0;
    for(var i = 0; i < list.length; i++){
        if(list[i].completed){
            completedCount++;
        }
    }
    var percent = (completedCount / list.length);
    return percent; 
}
%>
<body>
    <div id="page">
        <%- include("partials/sideBar.ejs") %>
        
        <div id="dashboard">
            
            <div id="schedule">
                <h2>Today's Schedule</h2>
            </div>

            <div id="workList" class="list">
                <form action="/submit" method="POST">
                    <input type="hidden" name="submitGroup" value="work">
                    <input type="text" name="taskName" placeholder="Add Work Task">
                    <input type="date" name="taskDay">
                    <input type="time" name="taskTime">
                    <input type="submit" value="+">
                </form>
                
                <ul class="list">
                    <% if (locals.work && work.length != 0) { %>
                        <form action="/checkActivity" method="POST">
                            <input type="hidden" name="submitGroup" value="work">
                            <% for (var i = 0; i < work.length; i++) { %>
                                <div>
                                    <input type="checkbox" name="<%=work[i].activity%>"
                                    <% if (work[i].completed) { %> 
                                        checked
                                    <% } %> >
                                    <label for="<%=work[i].activity%>"><%=work[i].activity%></label>
                                </div>    
                            <% } %>
                            <input type="submit" value="Submit Activities">
                        </form>

                    <%} else { %>
                        <div class="nothingBox">Currently Nothing</div>
                    <% } %>
                </ul>
            </div>
            
            <div id="progressRing">
                <svg width="250" height="250" viewBox="0 0 250 250">
                    <circle class="bg"
                        cx="125" cy="125" r="115" fill="none" stroke="#ddd" stroke-width="20"
                    ></circle>
                    <circle class="fg"
                        cx="125" cy="125" r="115" fill="none" stroke="#5394fd" stroke-width="20"
                        <% if (locals.work && work.length != 0) { %> 
                            stroke-dasharray="<%-progressCalc(work) * 2 * Math.PI * 115%> 
                            <%-722.5 - progressCalc(work) * 2 * Math.PI * 115%>"        
                        <%} else { console.log("I'm in else");%>
                            stroke-dasharray = "0 722.5"
                        <% } %>
                    ></circle>

                    <circle class="bg"
                    cx="125" cy="125" r="95" fill="none" stroke="#ddd" stroke-width="20"
                    ></circle>
                    <circle class="fg"
                    cx="125" cy="125" r="95" fill="none" stroke="#8be78b" stroke-width="20"
                    <% if (locals.day && day.length != 0) { %> 
                        stroke-dasharray="<%-progressCalc(day) * 2 * Math.PI * 95%> 
                        <%-596.9 - progressCalc(day) * 2 * Math.PI * 95%>"        
                    <%} else { %>
                        stroke-dasharray = "0 596.9"
                    <% } %>
                    ></circle>
                </svg>
            </div>

            <div id="dayList" class="list">
                <form action="/submit" method="POST">
                    <input type="hidden" name="submitGroup" value="day">
                    <input type="text" name="taskName" placeholder="Add Work Task">
                    <input type="date" name="taskDay">
                    <input type="time" name="taskTime">
                    <input type="submit" value="+">
                </form>

                <ul class="list">
                    <% if (locals.day && day.length != 0) { %>
                        <form action="/checkActivity" method="POST">
                            <input type="hidden" name="submitGroup" value="day">
                            <% for (var i = 0; i < day.length; i++) { %>
                                <div>
                                    <input type="checkbox" name="<%=day[i].activity%>" 
                                    <% if (day[i].completed) { %> 
                                        checked
                                    <% } %> >
                                    <label for="<%=day[i].activity%>"><%=day[i].activity%></label>
                                </div>
                            <% } %>
                            <input type="submit" value="Submit Activities">
                        </form>
                    <%} else { %>
                        <div class="nothingBox">Currently Nothing</div>
                    <% } %>
                </ul>
            </div>


        </div>
    </div>

</body>
</html>